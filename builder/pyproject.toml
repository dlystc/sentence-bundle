[project]
name = "builder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "UnknownMp", email = "unknownmp@163.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pyyaml>=6.0.3",
]

[project.scripts]
builder = "builder:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
